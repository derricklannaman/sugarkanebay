%h1= "#{current_user.firstname}'s Cart"
%hr/
.row
  .col-sm-6
    %ul
      - if current_user.cart
        = render partial: 'shared/ordered_items', locals: {orders: @orders}
        = render 'cart_total'
        = link_to "Proceed to Checkout", checkout_path

