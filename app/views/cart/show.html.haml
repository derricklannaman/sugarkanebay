.container
  .row
    %h1.text-center= "#{current_user.firstname}'s Cart"
    %hr/
.container
  .row
    - if current_user.cart
      = render partial: 'shared/ordered_items'
      = render 'cart_total'
      = link_to "Proceed to Checkout", checkout_path

